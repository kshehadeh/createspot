# Screenshot script: credentials for logging in when capturing help doc screenshots.
# Copy to .env and fill in. Required for: bun run capture-screenshots
# These are the same E2E vars used by the test suite (see root .env).
E2E_USER_EMAIL=
E2E_USER_PASSWORD=
E2E_BOOTSTRAP_SECRET=
# Optional: override the target URL (defaults to http://localhost:3000)
# SCREENSHOT_BASE_URL=http://localhost:3000
